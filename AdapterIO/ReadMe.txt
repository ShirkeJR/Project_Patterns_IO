1) Write an Class, which adds up two binary numbers from String;
	ex. '101' + '1' = '110'.
2) Write an Adapter, which converts 2 binary numbers (int) to string and adds up. 
   Use class to adding binary numbers from string. 
   Next converts on int and returns result.