1) Write an class, which  adds up two binary numbers from String;
	ex. '101' + '1' = '110'.
2) Write an adapter, which converts 2 binary numbers (int) on string and adds up. Next converts on int and return result.